service: email-service

provider:
  name: aws
  runtime: python3.9
  stage: dev

custom:
  serverless-offline:
    noPrependStageInUrl: true

plugins:
  - serverless-offline

functions:
  sendEmail:
    handler: handler.send_email
    events:
      - http:
          path: send
          method: post